============
Installation
============

At the command line::

    $ easy_install pyglobalgoals

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyglobalgoals
    $ pip install pyglobalgoals
